export enum CookiesEnum {
    LANGUAGE = 'language',
    OAUTHTOKEN = 'oauthToken',
    TOKEN = 'token',
}

export enum LanguagesEnum {
    EN = 'en',
    FR = 'fr',
}

export enum RoutesEnum {
    AREAS = '/areas',
    LOGIN = '/login',
    PROFILE = '/profile',
    REGISTER = '/register',
}
