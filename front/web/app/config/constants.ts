export enum CookiesEnum {
    LANGUAGE = 'language',
    OAUTHTOKEN = 'oauthToken',
    TOKEN = 'token',
}

export enum LanguagesEnum {
    EN = 'en',
    FR = 'fr',
}

export enum RoutesEnum {
    ABOUT = '/about',
    AREAS = '/areas',
    LOGIN = '/login',
    REGISTER = '/register',
}
