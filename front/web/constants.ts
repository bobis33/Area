export enum CookiesEnum {
    LANGUAGE = 'language',
    OAUTHTOKEN = 'oauthToken',
    TOKEN = 'token',
}

export enum LanguagesEnum {
    EN = 'en',
    FR = 'fr',
}

export enum RoutesEnum {
    ABOUT = '/about',
    AREAS = '/areas',
    HOME = '/home',
    LOGIN = '/login',
    REGISTER = '/register',
}